{% extends 'base.html.twig' %}

{% block title %}Recettes{% endblock %}

{% block body %}
    <h1>Recettes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ recette.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ recette.image }}</td>
            </tr>
            <tr>
                <th>TempsPreparation</th>
                <td>{{ recette.TempsPreparation }}</td>
            </tr>
            <tr>
                <th>TempsCuisson</th>
                <td>{{ recette.TempsCuisson }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ recette.Prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recettes_index') }}">back to list</a>

    <a href="{{ path('recettes_edit', {'id': recette.id}) }}">edit</a>

    {{ include('recettes/_delete_form.html.twig') }}
{% endblock %}
